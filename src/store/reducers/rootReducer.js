const iniState = {
    users: [
        {id:1, username:"Phuong"},
        {id:2, username:"Thanh Phuong"}
    ]
}
const rootReducer = (state = iniState,action) => { //state noi luu tru du lieu cua redux

    return state;
}
export default rootReducer;